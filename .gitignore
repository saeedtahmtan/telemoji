
# Environment (contains API keys + bot token)
.env

# Telethon session files
*.session

# Node modules
node_modules/

# Downloaded emoji artifacts already captured

# WebP tracked via Git LFS — pointer files are committed, blobs are offloaded
